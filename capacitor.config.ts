import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vladimirbeine.myapp',
  appName: 'myapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
